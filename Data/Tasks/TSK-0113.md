# TSK-0113: Add Drop-Resolution Table for Mined Blocks vs Inventory Items

**Priority:** P2 — Medium
**Status:** Backlog
**Sprint:** 47+
**Theme:** Correctness
**Source Audit:** `msa_code_audit_report-6-24-26.md` (Finding 3), `msa_sprint35-llm-first_audit_2026-06-24.md` (P0 Finding 1)

## Description

`WorldStateProjector.ApplyBlockMined` and `WorldModel.PredictMine` both add the mined block name directly as the inventory item key. This is correct for `oak_log → oak_log` but wrong for common cases like `stone → cobblestone`, `iron_ore → raw_iron`, etc. Planning, prediction, and completion checks can disagree on what a mining action produces, causing the agent to misread its own world state.

## Evidence

- **Validated by:** Code audit (88% confidence), sprint-35 audit (98% confidence)
- **Sources:**
  - `Agent.Core/WorldStateProjector.cs` — `ApplyBlockMined` adds `e.Block` directly
  - `Agent.Core/Models/WorldModel.cs` — `PredictMine` does the same
  - Codebase already has comments/tests showing stone→cobblestone logic in some places
- **Impact:** Gather completion can fail to trigger; predicted inventory diverges from actual; undermines uncertainty tracking and replanning quality

## Requirements

1. Introduce a single drop-resolution table (`Dictionary<string, string>` or `IBlockDropResolver`) used by both projection and prediction
2. Treat block name, mined result, and gathered target as distinct concepts
3. Add tests for at least one block with a non-identical drop result (e.g., stone→cobblestone) and verify both projection and prediction

## Acceptance Criteria

- [ ] `ApplyBlockMined` adds the correct drop item, not the raw block name
- [ ] `PredictMine` predicts the correct drop item
- [ ] Common mappings: stone→cobblestone, iron_ore→raw_iron, gold_ore→raw_gold, copper_ore→raw_copper, diamond_ore→diamond
- [ ] New tests cover at least stone→cobblestone projection and prediction
- [ ] All existing tests pass
- [ ] `dotnet build` passes with zero warnings

## Linked Items

- Related: TSK-0117 (post-craft/smelt reconciliation)
- Evidence: `Data/Pages/Audit/msa_code_audit_report-6-24-26.md`
- Evidence: `Data/Pages/Audit/msa_sprint35-llm-first_audit_2026-06-24.md`
