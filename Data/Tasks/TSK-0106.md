# TSK-0106: Add Error-Path Tests for catch→null and Fire-and-Forget

**Priority:** P2 — Medium
**Status:** Backlog
**Sprint:** 46 (Wave B)
**Theme:** Observability First
**Council:** audit-validation-sprint46-plan-council-2026-06-24

## Description

There are zero tests for any of the 7 catch→null paths in the codebase, and zero tests for the WebSocketBridge fire-and-forget receive loop error behavior. These are the exact paths that cause silent failures — and developers adding new LLM providers or planner paths have no coverage safety net.

## Locations to Test

| # | Location | Test Approach |
|---|---|---|
| 1 | AnthropicProvider catch→null | Mock HTTP handler throws → verify LogWarning called |
| 2 | GeminiProvider catch→null | Mock HTTP handler throws → verify LogWarning called |
| 3 | OpenAICompatibleProvider catch→null | Mock HTTP handler throws → verify LogWarning called |
| 4 | LlmChatInterpreter.ParseDecision catch→null | Inject malformed JSON → verify LogWarning called |
| 5 | LlmChatInterpreter.TryParseTruncatedJson catch→null | Inject unparseable truncated JSON → verify LogWarning called |
| 6 | HtnPlanner.ReplanAsync catch→null | Make planner throw during replan → verify LogWarning called |
| 7 | WebSocketBridge event deserialization catch→null | Inject malformed WebSocket message → verify LogWarning called |
| 8 | WebSocketBridge.ReceiveLoopAsync crash | Inject exception into loop body → verify reconnect attempt logged |

## Requirements

1. Add NUnit tests using `Mock<ILogger<T>>` to verify `LogWarning` is called at each catch→null location
2. Add test for WebSocketBridge ReceiveLoopAsync crash → reconnect attempt logged
3. Add test for WebSocketBridge ReceiveLoopAsync crash → after max retries → emergency stop triggered

## Acceptance Criteria

- [ ] 8+ new test methods covering all catch→null and fire-and-forget locations
- [ ] Each test uses `Mock<ILogger<T>>.Verify(l => l.Log(LogLevel.Warning, ...))` or equivalent
- [ ] `dotnet test` passes with zero failures
- [ ] `dotnet build` passes with zero warnings
