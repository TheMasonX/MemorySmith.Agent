# TSK-0109: Fix `/api/about` Metadata

**Priority:** P1 — High
**Status:** Done
**Sprint:** 46 (Wave C)
**Theme:** Tightening the Contracts
**Council:** sprint46-wavec-plan-council-2026-06-24

## Description

`/api/about` returns stale version and phase strings, undermining trust in runtime introspection. Currently reports `Version = "0.37.0"` and `Phase = "Sprint 36"`. Must reflect current roadmap state.

## Evidence

- **Source:** `WebUI.Blazor/Program.cs` — version string literal
- **Validated claims:** Deep dive audit finding 6 (99% confidence), addendum finding 5 (99% confidence), sprint-45 audit
- **Council:** Source-Grounded Archivist flagged this as "fastest fix with highest trust impact"

## Requirements

1. Update `Version` to `"0.46.0"` in the `/api/about` endpoint
2. Update `Phase` to `"Sprint 46 — Tightening the Contracts"`
3. Add a test that asserts the values match the expected roadmap version

## Stretch Goal

Centralize version/phase metadata in `Directory.Build.props` or a shared constants file so they can't drift again.

## Acceptance Criteria

- [ ] `/api/about` returns `Version = "0.46.0"` and `Phase = "Sprint 46 — Tightening the Contracts"`
- [ ] Test asserts the values match the roadmap
- [ ] `dotnet test` passes with zero failures
- [ ] `dotnet build` passes with zero warnings

## Linked Items

- Related: TSK-0105 (documentation drift — same theme)
- Evidence: `Data/Pages/council/sprint46-wavec-plan-council-2026-06-24.md`
